[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build -w @wedding/api"

[deploy]
startCommand = "npm run start -w @wedding/api"
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
